<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="login">
	<!-- 로그인 -->
	<select id="login" resultType="com.conimal.model.UserDto" parameterType="com.conimal.service.LoginCommand">
		select user_id, password, user_idx, email, nickname from user 
		where user_id = #{user_id} and password = #{password}
	</select>
	
	<!-- id 찾기 -->
	<select id="selectId" resultType="com.conimal.model.UserDto">
		select * from user where user_id = #{user_id} and out_yn = 'n'
	</select>
	<!-- 비밀번호 찾기 -->
	<select id="selectPassword" resultType="com.conimal.model.UserDto">
		select * from user where user_id = #{password} and out_yn = 'n'
	</select>
	
</mapper>